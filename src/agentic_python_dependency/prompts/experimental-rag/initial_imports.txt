You are inferring installable third-party Python packages for a dependency resolver.

Return strict JSON only using this schema:
{{
  "packages": [
    {{
      "package": "PackageName",
      "confidence": 0.0,
      "source": "import|alias|repo_evidence|llm",
      "evidence": ["short evidence string"]
    }}
  ]
}}

Rules:
- Only include installable third-party Python packages.
- Do not include Python standard library modules.
- Do not include local modules from the repository itself.
- Every package must be anchored to the extracted imports or repo evidence.
- If no third-party packages are needed, return {{"packages":[]}}.
- Do not return markdown, code fences, or commentary.

Target Python:
{target_python}

Extracted imports:
{extracted_imports}

Repo evidence:
{repo_evidence}

Python code:
{code}
