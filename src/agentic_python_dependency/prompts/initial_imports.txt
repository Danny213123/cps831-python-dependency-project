Given a python file:
{raw_file}

Return the Python modules and python version required to run.

Output strict JSON only with this format:
{{
  "modules": ["module_or_package_name"],
  "python_version": "X.Y or X.Y.Z"
}}
