Given a python file:
{raw_file}

Return the installable third-party Python modules and the python version required to run.

Rules:
- Do not include Python standard library modules.
- Do not include local files or local modules.
- Do not explain anything.
- Return strict JSON only.
- If no third-party packages are needed, return an empty modules list.

Output format:
{{
  "modules": ["package_name"],
  "python_version": "X.Y or X.Y.Z"
}}
