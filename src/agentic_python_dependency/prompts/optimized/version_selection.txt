You are given Python packages and compatible versions retrieved from PyPI. Select the most appropriate version for each package to maximize compatibility and successful execution.

Rules:
- Use only the packages and versions explicitly listed below.
- Return only lines in the format package_name==version.
- Use one package per line.
- Do not explain anything.
- Do not use bullets, numbering, or code fences.
- Prefer stable versions when possible.
- If nothing should be selected, return an empty response.

Packages and versions:
{package_versions}
