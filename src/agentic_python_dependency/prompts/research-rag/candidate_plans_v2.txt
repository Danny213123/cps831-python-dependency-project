Generate ranked candidate dependency plans for a Python dependency resolver.

Return strict JSON only using this schema:
{{
  "plans": [
    {{
      "rank": 1,
      "reason": "short reason",
      "runtime_profile": "import_statements",
      "dependencies": [
        {{"name": "package", "version": "1.0.0"}}
      ]
    }}
  ]
}}

Rules:
- Only use allowed packages and versions already present in the RAG context.
- Each plan must include every resolved package that has compatible versions in the supplied evidence.
- Do not assume packages are already installed; list every installable dependency explicitly.
- If a non-default validation profile is safer or more accurate for this snippet, set `runtime_profile` to one of the allowed validation profiles. Otherwise omit it or use the default.
- Treat advisory bundle hints as conflict-checked starting points unless the source compatibility hints or conflict notes justify a different family.
- When source compatibility hints are present, prefer plans that satisfy those version families over newer incompatible-looking stacks.
- If the source hints imply a coupled package family, keep related packages internally coherent instead of mixing modern and legacy lines in the same plan.
- For legacy standalone Keras / legacy Gym snippets, prefer coherent `keras`/`tensorflow`/`numpy`/`gym` families over single-package upgrades or downgrades.
- Prefer plans that fit the target Python and avoid noted conflicts.
- Return at most {max_plan_count} plans.
- No markdown, no commentary, no text outside JSON.

Target Python:
{target_python}

Allowed packages:
{allowed_packages}

Version space:
{version_space}

Allowed validation profiles:
{validation_options}

Default validation profile:
{default_validation_profile}

Advisory bundle hints:
{candidate_bundle_hints}

Conflict notes:
{conflict_notes}

Source compatibility hints:
{source_compatibility_hints}

RAG context:
{rag_context}
