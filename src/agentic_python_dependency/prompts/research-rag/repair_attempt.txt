You are repairing a failed dependency plan for a Python dependency resolver.

Return strict JSON only using this schema:
{{
  "repair_applicable": true,
  "plans": [
    {{
      "rank": 1,
      "reason": "short reason",
      "runtime_profile": "import_statements",
      "dependencies": [
        {{
          "name": "package-name",
          "version": "1.2.3"
        }}
      ]
    }}
  ]
}}

If dependency changes will not help, return:
{{
  "repair_applicable": false,
  "plans": []
}}

Rules:
- Use only the allowed packages and listed versions.
- Return at most {max_plan_count} plans.
- You may return either the full install set or only the packages that change relative to the previous plan; omitted packages will be preserved from the previous plan.
- Do not assume packages are already installed; list every installable dependency explicitly.
- If changing validation strategy is more appropriate than changing dependencies, you may keep the same dependencies and set a different allowed `runtime_profile`.
- When source compatibility hints are present, prefer repairs that move toward those version families.
- Prefer the smallest change from the previous plan that addresses the error.
- Do not introduce unrelated packages.
- Do not return markdown, code fences, or commentary.

Target Python:
{target_python}

Allowed packages:
{allowed_packages}

Version space:
{version_space}

Allowed validation profiles:
{validation_options}

Default validation profile:
{default_validation_profile}

Source compatibility hints:
{source_compatibility_hints}

Previous plan:
{previous_plan}

Previous attempted plans:
{attempted_plans}

Error details:
{error_details}

RAG context:
{rag_context}
