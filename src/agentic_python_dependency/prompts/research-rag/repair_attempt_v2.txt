You are revising dependency plans after a failed execution.

Return strict JSON only using this schema:
{{
  "repair_applicable": true,
  "plans": [
    {{
      "rank": 1,
      "reason": "short reason",
      "runtime_profile": "import_statements",
      "dependencies": [
        {{"name": "package", "version": "1.0.0"}}
      ]
    }}
  ]
}}

Rules:
- Only use allowed packages and versions already supported by the supplied evidence.
- You may return either the full install set or only the packages that change relative to the previous plan; omitted packages will be preserved from the previous plan.
- Do not assume packages are already installed; list every installable dependency explicitly.
- If changing validation strategy is more appropriate than changing dependencies, you may keep the same dependencies and set a different allowed `runtime_profile`.
- When source compatibility hints are present, prefer repairs that move toward those version families.
- When source compatibility hints span related packages, revise the coupled family together instead of downgrading or upgrading only one side of a constrained pair.
- If the error details show dependency conflicts between related packages, prefer a family-level repair that restores internal compatibility across those packages.
- Avoid repeating blocked strategy families from the repair memory.
- Prefer orthogonal fixes when prior strategies failed.
- If dependency changes are not likely to help, return {{"repair_applicable": false, "plans": []}}.
- No markdown, no commentary, no text outside JSON.

Target Python:
{target_python}

Allowed packages:
{allowed_packages}

Version space:
{version_space}

Allowed validation profiles:
{validation_options}

Default validation profile:
{default_validation_profile}

Source compatibility hints:
{source_compatibility_hints}

Previous plan:
{previous_plan}

Attempted plans:
{attempted_plans}

Error details:
{error_details}

Repair memory:
{repair_memory}

Feedback summary:
{feedback_summary}

RAG context:
{rag_context}
