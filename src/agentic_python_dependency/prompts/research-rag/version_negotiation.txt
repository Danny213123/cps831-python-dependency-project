Rank candidate dependency bundles for the best compatibility.

Return strict JSON only using this schema:
{{
  "selected_bundles": [
    {{
      "rank": 1,
      "reason": "short reason",
      "dependencies": [
        {{"name": "package", "version": "1.0.0"}}
      ]
    }}
  ]
}}

Rules:
- Only use the candidate bundles provided.
- Prefer bundles compatible with the target Python and repo evidence.
- Avoid bundles contradicted by the conflict notes.
- No markdown, no commentary, no text outside JSON.

Target Python:
{target_python}

Candidate bundles:
{candidate_bundles}

Conflict notes:
{conflict_notes}

Repo evidence:
{repo_evidence}
